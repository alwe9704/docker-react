language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t westmantech/docker-react -f Dockerfile.dev .
  
script:
  - docker run -e CI=true westmantech/docker-react npm run test
